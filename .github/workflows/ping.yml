name: Ping Render Backend

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes
  workflow_dispatch:       # manual run option

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl ping to Render
        run: curl -s https://perfume-helppage.onrender.com/ping
